<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <local:ZunDokoViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <RadioButton Content="ｽﾞﾝﾄﾞｺ" Margin="0,0,80,0"/>
            <RadioButton Content="ﾃｯﾃｰﾃﾚｯﾃｰ"/>
        </StackPanel>
        <Button Content="Start!" Grid.Row="1" Command="{Binding model.C_ZundokoStart, Mode=OneWay}"/>
        <ScrollViewer Grid.Row="2">
            <TextBlock TextWrapping="Wrap" TextAlignment="Center" Text="{Binding model._Zundoko.Value}"/>
        </ScrollViewer>

    </Grid>
</Window>
