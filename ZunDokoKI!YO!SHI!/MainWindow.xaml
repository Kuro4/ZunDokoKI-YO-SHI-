<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="Zundoko &amp; Tetteyterettey " Height="350" Width="525">
    <Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <RadioButton Content="ｽﾞﾝﾄﾞｺ" Margin="0,0,80,0" IsChecked="{Binding _isZundokoSelect}"/>
            <RadioButton Content="ﾃｯﾃｰﾃﾚｯﾃｰ" IsChecked="{Binding _isTetteyteretteySelect}"/>
        </StackPanel>
        <Button Content="Start!" Grid.Row="1" Command="{Binding C_Start, Mode=OneWay}"/>
        <ScrollViewer Grid.Row="2">
            <TextBlock TextWrapping="Wrap" TextAlignment="Center" Text="{Binding _Model._Result.Value}"/>
        </ScrollViewer>
    </Grid>
</Window>
